{
  "name": "hien-le-garden-landing",
  "private": true,
  "version": "1.0.0",
  "description": "Regression tests for the Hien Le Garden static landing pages (v3, v4).",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:v3": "playwright test --project=v3",
    "test:v4": "playwright test --project=v4"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "http-server": "^14.1.1"
  }
}
